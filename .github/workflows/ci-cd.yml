name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: Ejecutar pruebas
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout código
      uses: actions/checkout@v3

    - name: Configurar Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Instalar servidor HTTP para pruebas
      run: npm install -g http-server

    - name: Ejecutar pruebas
      run: |
        # Iniciar servidor en segundo plano
        http-server -p 8080 . &
        SERVER_PID=$!
        
        # Esperar a que el servidor inicie
        sleep 3
        
        # Ejecutar pruebas verificando el contenido HTML
        echo "Ejecutando pruebas..."
        
        # Prueba 1: Verificar que el título existe
        if curl -s http://localhost:8080/index.html | grep -q "Hola Mundo DevOps"; then
          echo "✅ Prueba 1 pasó: Título encontrado"
        else
          echo "❌ Prueba 1 falló: Título no encontrado"
          exit 1
        fi
        
        # Prueba 2: Verificar que el estado es correcto
        if curl -s http://localhost:8080/index.html | grep -q "funcionando correctamente"; then
          echo "✅ Prueba 2 pasó: Estado correcto"
        else
          echo "❌ Prueba 2 falló: Estado incorrecto"
          exit 1
        fi
        
        # Prueba 3: Verificar que la página contiene CI/CD
        if curl -s http://localhost:8080/index.html | grep -q "CI/CD"; then
          echo "✅ Prueba 3 pasó: Contenido CI/CD encontrado"
        else
          echo "❌ Prueba 3 falló: Contenido CI/CD no encontrado"
          exit 1
        fi
        
        # Detener servidor
        kill $SERVER_PID
        
        echo "🎉 Todas las pruebas pasaron correctamente"

  build-and-push:
    name: Construir y publicar imagen Docker
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && success()
    
    steps:
    - name: Checkout código
      uses: actions/checkout@v3

    - name: Login a Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Construir imagen Docker
      run: |
        docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/mi-app-html:latest .

    - name: Publicar imagen en Docker Hub
      run: |
        docker push ${{ secrets.DOCKERHUB_USERNAME }}/mi-app-html:latest
        echo "✅ Imagen publicada en Docker Hub: ${{ secrets.DOCKERHUB_USERNAME }}/mi-app-html:latest"

  deploy:
    name: Desplegar en Render
    runs-on: ubuntu-latest
    needs: build-and-push
    if: github.event_name == 'push' && success()
    
    steps:
    - name: Notificar despliegue
      run: |
        echo "🚀 Iniciando proceso de despliegue en Render"
        echo "📦 Imagen: ${{ secrets.DOCKERHUB_USERNAME }}/mi-app-html:latest"
        
        # En un escenario real, aquí se incluiría la lógica para desplegar en Render
        # usando la API de Render o el CLI oficial
        
        # Simulamos el despliegue con una espera
        sleep 10
        echo "✅ ¡Despliegue completado con éxito!"
        echo "🌐 La aplicación debería estar disponible en Render"
